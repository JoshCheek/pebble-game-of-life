{
  "private": true,
  "name": "playing-with-pebble",
  "scripts": {
    "test": "env NODE_PATH=src mocha",
    "exec": "exec",
    "env": "env"
  },
  "devDependencies": {
    "mocha": "~2.2"
  },
  "files": [
    "src"
  ]
}
